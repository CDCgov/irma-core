image: rustlang/rust:nightly

stages:
  - build
  - test

irma-build:
  stage: build
  tags:
    - docker
    - scicomp
  script:
    - cargo build --verbose

irma-test:
  stage: test
  tags:
    - docker
    - scicomp
  script:
    - cargo test --verbose

irma-docs:
  stage: test
  tags:
    - docker
    - scicomp
  script:
    - cargo doc --verbose
